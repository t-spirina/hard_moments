#pragma once
#include <iostream>
#include <algorithm>
#include <fstream>
#include <iterator>
#include <string>
#include <vector>

class CountFromFile
{
public:
    //CountFromFile():default;

    int32_t forFor(std::ifstream &file);
    int32_t counIfFind(std::ifstream &file);
    int32_t countIfFor(std::ifstream &file);
    int32_t countIfForfor(std::ifstream &file);
    int32_t forFind(std::ifstream &file);


};


